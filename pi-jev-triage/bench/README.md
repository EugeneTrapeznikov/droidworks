# Benchmarks

Raw results are not committed: no run output, transcripts, session files, or per-session tables.
Run the scripts in each tier (`swe/`, `session-replay/`) to reproduce; output lands in local, git-ignored `results/` directories.
Numbers in each `RESULTS.md` are aggregates only.
