diff --git a/packages/coding-agent/src/modes/interactive/tui-renderer.ts b/packages/coding-agent/src/modes/interactive/tui-renderer.ts
index 70d94231..21d2b473 100644
--- a/packages/coding-agent/src/modes/interactive/tui-renderer.ts
+++ b/packages/coding-agent/src/modes/interactive/tui-renderer.ts
@@ -25,0 +26 @@ export function createInteractiveTui(options: InteractiveTuiOptions): TuiMainScr
+			selectionStyle: (text) => theme.bg("selectedBg", theme.fg("text", text)),
diff --git a/packages/tui/src/tui-alt-screen.ts b/packages/tui/src/tui-alt-screen.ts
index 28a9e37a..b8d730d2 100644
--- a/packages/tui/src/tui-alt-screen.ts
+++ b/packages/tui/src/tui-alt-screen.ts
@@ -169,0 +170,2 @@ export interface TuiAltScreenOptions {
+	/** Style application-owned transcript selection text. */
+	selectionStyle?: (text: string) => string;
@@ -241,0 +244 @@ export class TuiAltScreen extends TuiBase implements ViewportTUI {
+	private readonly selectionStyle: (text: string) => string;
@@ -268,0 +272 @@ export class TuiAltScreen extends TuiBase implements ViewportTUI {
+		this.selectionStyle = options.selectionStyle ?? ((text) => `\x1b[7m${text}\x1b[27m`);
@@ -1548 +1552,2 @@ export class TuiAltScreen extends TuiBase implements ViewportTUI {
-		let result = "\x1b[7m";
+		let result = "";
+		let plainStart = 0;
@@ -1553 +1557,0 @@ export class TuiAltScreen extends TuiBase implements ViewportTUI {
-				result += text[index];
@@ -1556,0 +1561 @@ export class TuiAltScreen extends TuiBase implements ViewportTUI {
+			if (index > plainStart) result += this.selectionStyle(text.slice(plainStart, index));
@@ -1558 +1562,0 @@ export class TuiAltScreen extends TuiBase implements ViewportTUI {
-			if (ansi.code.endsWith("m")) result += "\x1b[7m";
@@ -1559,0 +1564 @@ export class TuiAltScreen extends TuiBase implements ViewportTUI {
+			plainStart = index;
@@ -1561 +1566,2 @@ export class TuiAltScreen extends TuiBase implements ViewportTUI {
-		return `${result}\x1b[27m`;
+		if (plainStart < text.length) result += this.selectionStyle(text.slice(plainStart));
+		return result;
